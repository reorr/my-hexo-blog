extends partial/layout

block site_title
    !=  "归Archive · " + config.title

block description
    - var desc = 'Post Archives - ' + config.title;
    meta(name="description", content=desc)

block content
    .autopagerize_page_element: .content
      .archive.animated.fadeInDown
        ul.list-with-title
          include mixins
          +postList()
          +make_pager(__('prev'), __('next'))
